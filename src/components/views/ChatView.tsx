// ──────────────────────────────────────────────────────────────────────
// ChatView — the 'Chat' primary tab content (inside the TabsPanel, not
// to be confused with the always-visible ChatColumn on the left).
// Placeholder until a later chapter builds the in-panel chat view.
// ──────────────────────────────────────────────────────────────────────

import ComingSoon from './ComingSoon'

export default function ChatView() {
  return <ComingSoon label="Chat" />
}
